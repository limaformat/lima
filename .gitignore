# JavaScript
node_modules/
# Build artifact: rebuilt by `prepack`/`pretest` and in CI; not a source of truth.
js/dist/
editors/lsp/dist/

# Rust
rust/target/
rust/Cargo.lock

# OS / editor
.DS_Store
*.log
