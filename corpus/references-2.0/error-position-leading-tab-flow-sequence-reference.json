{
  "id": "references-2.error-position.leading-tab-flow-sequence-reference",
  "spec": "references",
  "section": "2.4",
  "description": "A flow sequence on a hard-tab-indented nested key line. The tab expands to two spaces under Core §3, but §2.4 anchors the token to the original source: the `$` inside `[${missing}]` reports column 10 (1 tab + `inner: [`), not column 11 as measured after tab expansion. Exercises tab-adjust propagation through the flow-element source anchor.",
  "input": "outer:\n\tinner: [${missing}]\n",
  "options": {
    "strict": true
  },
  "expect": {
    "error": {
      "line": 2,
      "token": "${missing}",
      "code": "UNRESOLVED_REFERENCE",
      "column": 10
    }
  },
  "tags": [
    "error-order",
    "error"
  ],
  "specVersion": "2.0"
}
