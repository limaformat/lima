{
  "id": "references-2.partials.mapping-path.invalid-intermediate.nonstrict",
  "spec": "references",
  "specVersion": "2.0",
  "section": "3.1",
  "description": "Traversal through a non-mapping partial value leaves the complete token unchanged in non-strict mode.",
  "input": "name: $(author.name)\n",
  "options": { "partials": { "author": "Ada" } },
  "expect": { "result": { "name": "$(author.name)" } },
  "tags": ["fallback", "partial", "dotted-path"]
}
