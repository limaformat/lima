{
  "id": "references-2.error-position.escaped-hash-flow-sequence",
  "spec": "references",
  "section": "2.4",
  "description": "The §2.4 physical-position rule holds inside a flow sequence: a `\\#` collapses to `#` in the decoded element but the token keeps its original-source column. `$` is at column 10 of the source line.",
  "input": "x: [a\\#b ${missing}]\n",
  "options": {
    "strict": true
  },
  "expect": {
    "error": {
      "line": 1,
      "token": "${missing}",
      "code": "UNRESOLVED_REFERENCE",
      "column": 10
    }
  },
  "tags": [
    "error-order",
    "error"
  ],
  "specVersion": "2.0"
}
