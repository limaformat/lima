{
  "id": "core.keys.top-level.dot-invalid",
  "spec": "core",
  "section": "5.1 (1.0.5)",
  "description": "§5.1: an unquoted key matches [a-zA-Z0-9_][a-zA-Z0-9_:-]*. A dotted key is not a key; the line is unrecognised and skipped. §10's strict list is closed and does not cover a malformed top-level key, so strict does not throw either.",
  "input": "a.b: value\n",
  "options": { "strict": true },
  "expect": {
    "result": {}
  },
  "tags": ["strict", "keys"]
}
