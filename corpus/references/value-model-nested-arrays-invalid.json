{
  "id": "references.value-model.nested-arrays-invalid",
  "spec": "references",
  "section": "6.2",
  "description": "A partial array directly containing another array as an element is invalid — no nested arrays in the Lima Value Model.",
  "input": "a: (%arr)",
  "options": { "partials": { "arr": [[1, 2], [3, 4]] } },
  "expect": {
    "error": {
      "partial": "arr",
      "code": "INVALID_PARTIAL"
    }
  },
  "tags": ["error", "value-model"]
}
